#!/usr/bin/env python3

import sys
import locale
import gettext

localedir = '@localedir@'

locale.bindtextdomain('seneca', localedir)
locale.textdomain('seneca')
gettext.bindtextdomain('seneca', localedir)
gettext.textdomain('seneca')

def main():
    sys.path.insert(1, '@pythondir@')
    from seneca import application
    app = application.Application()

    try:
        ret = app.run(sys.argv)
    except SystemExit as e:
        ret = e.code

    sys.exit(ret)

if __name__ == '__main__':
    main()
